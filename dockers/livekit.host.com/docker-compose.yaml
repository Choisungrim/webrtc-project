# This docker-compose requires host networking, which is only available on Linux
# This compose will not function correctly on Mac or Windows
services:
  caddy:
    image: livekit/caddyl4
    command: run --config /etc/caddy.yaml --adapter yaml
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./caddy.yaml:/etc/caddy.yaml
      - ./livekit.host.com+1.pem:/etc/livekit/certs/livekit.host.com+1.pem
      - ./livekit.host.com+1-key.pem:/etc/livekit/certs/livekit.host.com+1-key.pem
      - caddy_data:/data
    depends_on:
      - livekit
  livekit:
    image: livekit/livekit-server:latest
    command: --config /etc/livekit.yaml
    restart: unless-stopped
    ports:
      - "7880:7880"
      - "7881:7881"
    volumes:
      - ./livekit.yaml:/etc/livekit.yaml

volumes:
  caddy_data:
